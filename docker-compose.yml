version: '3.8'

services:
  sora2api:
    build: .   # ← 用本地代码构建镜像（非常重要）
    container_name: sora2api
    ports:
      - "8000:8000"
    volumes:
      - ./:/app   # ← 暴露整个项目目录
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
